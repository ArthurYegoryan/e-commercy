const ProductTitleArea = ({ title }) => {
    return (
        <div className="product-title-area">
            <p className="product-title">{title}</p>
        </div>
    );
};

export default ProductTitleArea;