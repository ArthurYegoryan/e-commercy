import logoImage from "../img/logo.webp";

const Logo = () => {
    return (
        <div className="logo">
            <img src={logoImage} alt="logo" />
        </div>
    );
};

export default Logo;